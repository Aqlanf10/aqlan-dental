[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "dotnet AqlanDentalPro.API.dll"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
