# Requirements Template

Spec folder: `specs/<id-name>/`

## Requirement

- Requirement ID:
- User story:
- Current evidence from repo:
- Business rule:
- Arabic UI text, if applicable:
- Permissions:
- Data rules:
- Runtime verification needed: Yes/No. If yes, write `Needs runtime verification`.

## Acceptance Criteria

- WHEN `<event>` THEN the system SHALL `<expected behavior>`.
- WHEN `<error or forbidden action>` THEN the system SHALL show an Arabic `message` and preserve existing data.

## Notes

- Existing owner route:
- Backend owner:
- Frontend owner:
- Tests to inspect or update:
