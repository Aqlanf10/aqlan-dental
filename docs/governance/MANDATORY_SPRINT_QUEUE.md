# طابور السبرنتات الإلزامي — Mandatory Sprint Queue

**هذا الملف هو مصدر الحقيقة الوحيد لترتيب العمل على Aqlan Dental Pro.**
يلغي أي ترتيب سابق في ملفات الخطط التاريخية. الأدلة التفصيلية تبقى في
`docs/governance/SPEC_TO_CODE_TRACEABILITY.md` وملفات التدقيق والـPRs المرتبطة.

## القاعدة الإلزامية

1. قبل أي تطوير، اقرأ هذا الملف كاملًا.
2. أول عنصر بحالة `🔴 محجوب` أو `🟡 جاهز` أو `🔵 قيد التنفيذ` هو العمل الوحيد المسموح.
3. لا يجوز القفز لعنصر لاحق إلا لحادث إنتاج P0، تدقيق قراءة فقط، إكمال PR مفتوح، أو حل تعارض دمج.
4. كل PR تنفيذي يذكر رقم العنصر في العنوان، ويحدّث حالته عند تحقق معيار الخروج.
5. البنود المنجزة لا يعاد فتحها إلا بدليل ارتداد أو عطل إنتاجي جديد.

---

## الطابور بالترتيب

### ✅ SEQ-00 — فك توقف نشر Railway
- **الدليل:** نشر الإنتاج عاد للعمل وتحقق endpoint التشخيص بلا failures.

### ✅ SEQ-01 — أمان جلسات بوابة المرضى
- refresh token داخل HttpOnly/Secure/SameSite cookie، ولا تخزين للتوكن في localStorage.

### ✅ SEQ-02 — تعيين غرف الأطباء
- `DefaultClinicRoomId` + `DoctorRoomResolver` + التكامل مع الطابور والواجهة.

### ✅ SEQ-03 — إدارة المستخدمين والصلاحيات الموحدة
- تبويب إعدادات واحد قابل للربط العميق، ومسارات legacy تعيد التوجيه إليه، Admin-only.

### ✅ SEQ-04 — تفكيك `FinanceService`
- **PRs:** #626، #627، #628، وتوثيق #629.
- `FinanceService`/`IFinanceService` تقاعدا بالكامل لصالح ست خدمات متخصصة.

### ✅ SEQ-05 — توحيد يوم العيادة بتوقيت اليمن
- **PRs:** #630، #631.
- استبدال حسابات UTC الخاطئة بـ`ClinicTimeProvider` واختبارات حدود منتصف الليل.

### ✅ SEQ-06 — إصلاح ثلاثة أعطال 500 حية
- **PRs:** #632–#634.
- إصلاح `PackageId` startup DDL وتحويل enums في `OperationalExpense`.

### ✅ SEQ-07 — رسائل أخطاء التشغيل اليومي
- **PR:** #635.
- توحيد `extractErrorMessage` بدل التوستات العامة التي تخفي رسالة الخادم.

### ✅ SEQ-08 — تصليب وحدة التقويم
- **PRs:** #636، #639، #640، #645.
- حالات خطأ مرئية، إصلاحات توقيت، سلامة AI draft، واختبارات عدم الارتداد.

### ✅ SEQ-09 — تنظيف PRs القديمة الخطرة
- **PR:** #638.
- إغلاق #363 و#407؛ حذف الفروع الخاملة مؤجل بقرار المالك ولا يحجب العمل.

### ✅ SEQ-10 — إصلاح سباق `UPLOADS_PATH` في الاختبارات
- **PR:** #638.
- تسلسل كل مختبري المتغير العام داخل collection واحدة.

### ✅ SEQ-11 — تصليب الصفحات عالية الاستخدام
- **PR:** #646.
- منع الأصفار/الحالات الفارغة الكاذبة والنجاح الزائف في المواعيد والمختبر والمرضى.

### ✅ SEQ-12 — تثبيت SEQ-11 وإكمال FE-11
- **PR:** #647.
- 203/203 اختبار Frontend، واكتمال توحيد استخراج رسائل الأخطاء.

### ✅ SEQ-13 — جعل المنطقة الزمنية قابلة للتهيئة بأمان
- **PR:** #648.
- المصدر: `Settings[clinic.timezone]` ثم config/env ثم fallback `Asia/Aden`.
- قراءة واحدة عند الإقلاع، cache آمن، fallback مع warning، وتوحيد `AppointmentReminderJob`.
- **معيار الخروج:** CI وEncoding Guard أخضران، ملاحظة Codex P2 عولجت، ولا migration أو API change.

### ✅ SEQ-14 — إصلاح انحراف DTO في السيفالو
- **PR:** #650.
- أصبحت حقول `IsAutoTraced` و`DoctorId` و`Notes` ممثلة في `CephAnalysis` بالواجهة.
- تعرض شاشة التفاصيل الملاحظات السريرية المحفوظة، وتفك غلاف JSON الخاص بالمعايرة بدل إظهار البيانات الخام.
- يظهر وسم واضح للتحليل الناتج من التتبع الآلي مع اشتراط مراجعة الطبيب.
- **معيار الخروج:** اختبارات type/render تغطي النص العادي، JSON المغلف، الحالة الفارغة، ووسم التتبع الآلي؛ لا schema أو API أو تعديل سريري تلقائي.

### ✅ SEQ-15 — تصحيح دلالة ملاحظات السيفالو عند حد DTO
- **PR:** #651.
- أصبح `CephAnalysisDetailDto.Notes` يعيد الملاحظة السريرية فقط بدل غلاف JSON الداخلي للمعايرة.
- يدعم النصوص القديمة و`UserNotes` بصيغتي PascalCase/camelCase، ويعيد `null` للغلاف الخالي دون تعديل القيمة المخزنة.
- **معيار الخروج:** Backend وFrontend وE2E وEncoding Guard خضراء، واختبارات الخدمة/DTO تغطي النص القديم والغلاف والحالة الفارغة وJSON غير المكتمل، دون schema أو endpoint change.

### ✅ SEQ-16 — منع الحالة الفارغة الكاذبة في عيادة الطبيب
- **PR:** #652.
- فشل التحميل الأول لقائمة مرضى عيادة الطبيب يعرض شاشة خطأ عربية واضحة بدل «لا يوجد مرضى» الكاذبة.
- فشل التحديث الدوري بعد وجود بيانات لا يهدم مساحة الطبيب ولا يفقد الملاحظات السريرية غير المحفوظة؛ تبقى البيانات المخزنة ظاهرة.
- **معيار الخروج:** الاستجابة الناجحة الفارغة، والفشل الأولي، وفشل التحديث الخلفي مغطاة باختبارات ارتداد؛ Backend وFrontend وE2E وEncoding Guard خضراء، وملاحظة Codex P2 محلولة، ولا API أو schema أو تغيير في سير الزيارة.

### ✅ SEQ-17 — كشف حساب الطبيب غير المرتبط بسجل طبيب
- **PR:** #653.
- الحساب السريري غير المرتبط بـ`doctorId` لا يرسل طلب قائمة مرضى ولا يعرض «لا يوجد مرضى» الكاذبة، بل تظهر له رسالة إعداد عربية تطلب من المدير ربط الحساب من إعدادات المستخدمين والصلاحيات.
- بقي وضع Admin لعرض جميع الأطباء، وبقي الحساب المرتبط يرسل `doctorId` الصحيح دون تغيير في الوصول أو سير الزيارة.
- **معيار الخروج:** اختبارات الواجهة تغطي الحساب غير المرتبط، الحساب المرتبط، ورسالة الإصلاح؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو schema أو auth DTO أو ربط تلقائي.

### ✅ SEQ-18 — إثراء استجابة إنشاء الموعد
- **PR:** #655.
- أصبحت معاملة إنشاء الموعد تحمّل علاقات `Patient` و`Doctor` و`Package` قبل الإرجاع، لذلك تحتوي الاستجابة الفورية أسماء العرض الصحيحة بدل القيم الفارغة.
- بقي فحص التعارض والقفل الذري كما هو، وتحميل العلاقات داخل المعاملة يمنع حفظ الموعد ثم إعادة 500 إذا فشل الإثراء.
- **معيار الخروج:** اختبار repository يبدأ بموعد يحوي المعرّفات فقط ويثبت تحميل بيانات المريض والطبيب؛ Backend وFrontend وE2E وEncoding Guard خضراء، ولا schema أو API أو قواعد حجز أو صلاحيات تغيرت.

### ✅ SEQ-19 — توحيد قائمة مواعيد اليوم مع تاريخ العيادة
- **PR:** #656.
- أصبحت `AppointmentRepository.GetTodayAsync` تستخدم `ClinicTimeProvider.ClinicToday()` بدل تاريخ خادم التشغيل، مع تاريخ صريح اختياري للاختبارات الحتمية.
- بقيت فلاتر الفرع والطبيب وترتيب الموعد والتحميل المسبق للمريض والطبيب دون تغيير.
- **معيار الخروج:** اختباران يغطيان التاريخ الصريح والافتراضي المهيأ؛ Backend وFrontend وE2E وEncoding Guard خضراء، ولا endpoint أو DTO أو صلاحيات أو قواعد حجز تغيرت.

### ✅ SEQ-20 — تنبيهات لوحة التحكم لا تختفي بصمت عند فشل التحميل
- **PR:** #657.
- **الدليل:** `frontend/src/components/dashboard/AttentionAlerts.tsx` يعيد `null` عند فشل `/api/dashboard/alerts`، ويخفي كذلك التنبيهات المخبأة عند فشل تحديث خلفي.
- **النطاق:** حالة خطأ عربية مدمجة مع إعادة المحاولة ورسالة الخادم الآمنة، مع إبقاء بيانات التنبيهات المخبأة ظاهرة. لا تغيير في عقد API.
- **معيار الخروج:** خمسة اختبارات واجهة تغطي النجاح مع تنبيهات، النجاح الفارغ، الفشل الأولي، فشل التحديث الخلفي مع بقاء البيانات، وإعادة المحاولة؛ Backend وFrontend وE2E وEncoding Guard وVercel خضراء.

### ✅ SEQ-21 — أحدث المرضى لا تعرض حالة فارغة أثناء التحميل
- **PR:** #658.
- **الدليل:** `frontend/src/app/(dashboard)/page.tsx` يبدأ `recentPatients` كمصفوفة فارغة بلا حالة تحميل مستقلة، فيعرض «لا يوجد مرضى بعد» بينما طلب `/api/patients?pageSize=5&status=active` ما زال معلقًا.
- **النطاق:** حالة تحميل مدمجة لقسم أحدث المرضى، مع إبقاء حالتي النجاح الفارغ والخطأ الحاليتين. لا تغيير في API أو وصول المرضى.
- **معيار الخروج:** اختبار ارتداد يؤخر استجابة المرضى ويثبت ظهور حالة التحميل وعدم ظهور الحالة الفارغة حتى نجاح الطلب؛ Backend وFrontend وE2E وEncoding Guard وVercel خضراء.

### ✅ SEQ-22 — فشل تحميل العقود لا يظهر كقائمة عقود فارغة
- **PR:** #659.
- **الدليل:** عند حجب `/api/finance-v3/contracts` في الإنتاج، تعرض تبويبة العقود رسالة «فشل في تحميل العقود» وفي الوقت نفسه «لا توجد عقود»، لأن `ContractsTab` لا يحتفظ بحالة خطأ مستقلة.
- **النطاق:** حالة خطأ عربية مدمجة مع إعادة المحاولة، ومنع الحالة الفارغة الكاذبة، وإبقاء بيانات العقود المخزنة ظاهرة عند فشل تحديث لاحق. لا تغيير في API أو الحسابات المالية.
- **معيار الخروج:** اختبارات واجهة تغطي الفشل الأولي، إعادة المحاولة، وفشل التحديث مع بقاء البيانات؛ Frontend وBackend وE2E وEncoding Guard وVercel خضراء.

### ✅ SEQ-23 — أخطاء الشبكة العامة تستخدم الرسالة العربية الاحتياطية
- **PR:** #660.
- **الدليل:** تحقق SEQ-22 بعد النشر أظهر `Network Error` داخل حالة الخطأ والتنبيه بدل «فشل في تحميل العقود» عند انقطاع الشبكة.
- **النطاق:** تجعل `extractErrorMessage` رسالتي النقل العامتين `Network Error` و`Failed to fetch` تستخدمان fallback الممرر، مع إبقاء رسائل الخادم والأخطاء المحددة دون تغيير.
- **معيار الخروج:** اختبارات الوحدة تغطي رسالتي الشبكة والرسالة المحددة؛ جميع فحوص CI والنشر خضراء، والتحقق الحي يعرض العربية.

### ✅ SEQ-24 — فشل تحميل الباقات لا يظهر ككتالوج فارغ
- **PR:** #661.
- **الدليل:** عند حجب `/api/treatment-packages` في الإنتاج، تعرض `/settings/packages` «لا توجد باقات» و«أضف أول باقة» بلا رسالة خطأ أو إعادة محاولة.
- **النطاق:** حالة خطأ عربية مدمجة مع إعادة المحاولة، ومنع الحالة الفارغة الكاذبة، وإبقاء الباقات المحملة ظاهرة عند فشل تحديث لاحق. لا تغيير في API أو بيانات الباقات.
- **معيار الخروج:** اختبارات واجهة تغطي الفشل الأولي، إعادة المحاولة، وفشل التحديث مع بقاء البيانات؛ Frontend وBackend وE2E وEncoding Guard وVercel خضراء.

### ✅ SEQ-25 — فشل تحميل قوالب الوثائق لا يظهر كقائمة فارغة
- **PR:** #662.
- **الدليل:** عند حجب `/api/document-templates` في الإنتاج، تعرض `/settings/templates` رسالة الفشل و«لا توجد قوالب بعد» معًا، بلا إعادة محاولة.
- **النطاق:** فصل خطأ تحميل القائمة عن أخطاء النموذج، وإضافة إعادة المحاولة، ومنع الحالة الفارغة الكاذبة، وإبقاء القوالب المحملة عند فشل تحديث لاحق. لا تغيير في API أو محتوى القوالب.
- **معيار الخروج:** اختبارات واجهة تغطي الفشل الأولي، إعادة المحاولة، وفشل التحديث مع بقاء البيانات؛ جميع فحوص CI والنشر خضراء.

### ✅ SEQ-26 — منع نتائج بحث مريض قديمة من استبدال البحث الأحدث
- **PR:** #663.
- يلغي `PatientCombobox` الطلب السابق ويستخدم رقمًا تسلسليًا، ويتجاهل النجاح أو الفشل القديم، ولا يبقي نتائج سابقة قابلة للنقر بعد فشل البحث الحالي.
- **معيار الخروج:** أربعة اختبارات واجهة تغطي النجاح القديم، الفشل القديم، فشل البحث الحالي بعد نجاح سابق، وتقصير النص أثناء طلب قائم؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو صلاحيات أو schema تغيرت.

### ✅ SEQ-27 — منع نتائج البحث السريع القديمة من استبدال البحث الأحدث
- **PR:** #664.
- يفصل `TopbarSearch` البحث السريع عن الشريط، ويلغي الطلب السابق ويستخدم رقمًا تسلسليًا، ولا يعيد فتح قائمة أغلقها المستخدم، ويعرض فشل الشبكة بوضوح بدل «لا توجد نتائج».
- **معيار الخروج:** ستة اختبارات واجهة تغطي النجاح والفشل القديمين، فشل البحث الحالي، المسح، إغلاق القائمة أثناء الطلب، والتنقل للنتيجة؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو صلاحيات أو مسارات تغيرت.

### ✅ SEQ-28 — فشل تحميل الإشعارات لا يظهر كقائمة فارغة
- **PR:** #665.
- يعرض `TopbarNotifications` خطأ عربيًا مع إعادة المحاولة، ولا يمسح الإشعارات المحمّلة أو العدد غير المقروء عند فشل تحديث لاحق، ولا يعرض «لا توجد إشعارات» إلا بعد نجاح فارغ.
- **معيار الخروج:** خمسة اختبارات واجهة تغطي الفشل الأولي، إعادة المحاولة، النجاح الفارغ، فشل التحديث مع بقاء البيانات، والتنقل من الإشعار؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو صلاحيات أو schema تغيرت.

### ✅ SEQ-29 — عمليات الإشعارات لا تعرض نجاحًا زائفًا
- **PR:** #666.
- لا تتغير القائمة أو عداد غير المقروء إلا بعد نجاح الخادم في تعليم إشعار/الكل كمقروء أو الحذف، وتظهر رسالة خطأ عربية عند الفشل، ويمنع قفل متزامن تكرار العملية بالنقر السريع.
- **معيار الخروج:** اختبارات تغطي نجاح وفشل تعليم الكل، فشل تعليم إشعار مفرد، نجاح وفشل الحذف مع مزامنة العداد، وقفل العملية؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو schema أو صلاحيات تغيرت.

### ✅ SEQ-30 — منع طلبات تحميل الإشعارات القديمة من استبدال الأحدث
- **PR:** #667.
- يلغي `TopbarNotifications` الطلب السابق ويستخدم رقمًا تسلسليًا، ويلغي التحميل عند الإغلاق الخارجي أو إزالة المكوّن، فلا يستطيع نجاح أو فشل قديم استبدال الحالة الأحدث.
- **معيار الخروج:** اختبارات تغطي النجاح القديم بعد الحديث، الفشل القديم بعد نجاح حديث، الإغلاق الخارجي أثناء الطلب، والإزالة أثناء التحميل؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو schema أو صلاحيات تغيرت.

### ✅ SEQ-31 — منع تحميل قديم من التراجع عن عملية إشعار ناجحة
- **PR:** #668.
- يُبطل `TopbarNotifications` التحميل الجاري بعد نجاح حذف الإشعار أو تعليمه/تعليم الكل كمقروء، فلا يستطيع رد أقدم استعادة الحالة السابقة، ولا يُبطل التحميل عند فشل العملية.
- **معيار الخروج:** اختباران يثبتان عدم عودة الإشعار بعد الحذف وعدم عودة حالة غير المقروء بعد تعليم الكل؛ Frontend وBackend وE2E وEncoding Guard خضراء، ولا API أو schema أو صلاحيات تغيرت.

---

## آلية التحديث

- عند اكتشاف عمل جديد: أضفه في آخر الطابور بحالة `🟡` أو `🔴`، ولا تنفذه قبل البنود السابقة.
- عند اكتمال عنصر: غيّر حالته إلى `✅` مع PR ومعيار خروج مختصر.
