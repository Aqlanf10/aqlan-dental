"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { useQuery } from "@tanstack/react-query";
import Link from "next/link";
import {
  User, MessageSquare, Camera, Scan, Smile, Stethoscope, Image as ImageIcon,
  Ruler, Microscope, Sigma, ListChecks, ClipboardList, Target, GitBranch,
  FileText, Wrench, CalendarClock, Trophy, Shield, Presentation,
  CheckCircle2, CircleDashed, AlertCircle, BadgeCheck, ChevronLeft, Loader2,
  Upload, Pencil, Sparkles, ArrowUpCircle, Clipboard,
} from "lucide-react";
import api from "@/lib/api";
import { orthoService, type OrthoPresentationDefinition } from "@/services/orthoService";
import { useOrthoOverview, useOrthoPhotos } from "@/hooks/useOrtho";
import { resolveImageUrl } from "@/hooks/useClinicBranding";
import { toast } from "@/stores/toastStore";
import { cn } from "@/lib/utils";
import type { OrthoPhoto } from "@/types/ortho";
import { OrthoImagePreparationDialog } from "./OrthoImagePreparationDialog";

type StepStatus = "missing" | "partial" | "complete" | "approved";
type StepPriority = "critical" | "supporting" | "optional";
type DraftKind = "problems" | "diagnosis" | "objectives" | "strategies" | "plan" | "mechano";
type Tab = "overview" | "exam" | "cast" | "ceph" | "facial" | "problems" | "diagnosis" | "plan" | "stages" | "visits" | "retention" | "records" | "reports";
interface PhotoSlot { key: string; label: string; category: string; subtype: string; phase: string; capture: "environment" | "user"; }
interface ServerDraftResponse { draft: string; disclaimer?: string; evidenceUsed?: string[]; missingData?: string[]; warnings?: string[]; }
interface WizardStep { order: number; key: string; title: string; required: string; minimum: string; doctorAction: string; icon: typeof User; kind: "patient" | "data" | "photos" | "generate"; priority: StepPriority; tab?: Tab; slideTypes?: string[]; approvedFlag?: "diagnosis" | "plan"; photoSlots?: PhotoSlot[]; draftKind?: DraftKind; }

function s(order: number, key: string, title: string, required: string, minimum: string, doctorAction: string, icon: typeof User, kind: WizardStep["kind"], priority: StepPriority, tab?: Tab, slideTypes?: string[]): WizardStep { return { order, key, title, required, minimum, doctorAction, icon, kind, priority, tab, slideTypes }; }
const photoSlotsExtra: PhotoSlot[] = [{ key: "exf", label: "أمامي", category: "Extraoral", subtype: "Frontal", phase: "Initial", capture: "user" }, { key: "exp", label: "جانبي", category: "Extraoral", subtype: "Profile", phase: "Initial", capture: "user" }, { key: "exs", label: "ابتسامة", category: "Extraoral", subtype: "Smile", phase: "Initial", capture: "user" }];
const photoSlotsIntra: PhotoSlot[] = [{ key: "inf", label: "أمامي", category: "Intraoral", subtype: "Frontal", phase: "Initial", capture: "environment" }, { key: "inr", label: "يمين", category: "Intraoral", subtype: "Right", phase: "Initial", capture: "environment" }, { key: "inl", label: "يسار", category: "Intraoral", subtype: "Left", phase: "Initial", capture: "environment" }, { key: "inu", label: "علوي", category: "Intraoral", subtype: "Upper Occlusal", phase: "Initial", capture: "environment" }, { key: "ind", label: "سفلي", category: "Intraoral", subtype: "Lower Occlusal", phase: "Initial", capture: "environment" }];
const STEPS: WizardStep[] = [
  s(1, "patient", "بيانات المريض", "الاسم، رقم الملف، العمر، الجنس", "تأكد من البيانات الأساسية.", "افتح ملف المريض.", User, "patient", "critical", undefined, ["PatientInformation"]),
  s(2, "complaint", "المقابلة والشكوى", "الشكوى الرئيسية والتاريخ", "أدخل الشكوى والتاريخ.", "افتح الفحص السريري.", MessageSquare, "data", "critical", "exam", ["ChiefComplaint"]),
  { ...s(3, "extraoral", "الصور خارج الفم", "أمامي، جانبي، ابتسامة", "التقط الصور الأساسية.", "التقط ثم جهّز.", Camera, "photos", "critical", undefined, ["ExtraoralPhotos"]), photoSlots: photoSlotsExtra },
  s(4, "facial", "التحليل الوجهي", "الملف والأنسجة الرخوة", "أدخل التحليل عند توفره.", "افتح تحليل الصور.", Smile, "data", "supporting", "facial", ["FacialAnalysis"]),
  { ...s(5, "intraoral", "الصور داخل الفم", "أمامي، يمين، يسار، علوي، سفلي", "التقط الصور الخمس.", "استخدم الكاميرا الخلفية.", Scan, "photos", "critical", undefined, ["IntraoralPhotos"]), photoSlots: photoSlotsIntra },
  s(6, "occlusion", "تقييم الإطباق", "العلاقات، OJ/OB، الازدحام", "سجل الإطباق.", "افتح الفحص.", Stethoscope, "data", "critical", "exam", ["OcclusionAssessment"]),
  { ...s(7, "pano", "الأشعة البانورامية", "OPG", "ارفع البانوراما.", "ارفع الصورة.", ImageIcon, "photos", "supporting", undefined, ["PanoramicXray"]), photoSlots: [{ key: "opg", label: "بانوراما", category: "Radiograph", subtype: "OPG", phase: "Initial", capture: "environment" }] },
  { ...s(8, "cephxray", "الأشعة السيفالومترية", "سيفالو جانبي", "أضف السيفالو.", "افتح وحدة السيفالو.", ImageIcon, "photos", "supporting", undefined, ["CephalometricSummary"]), photoSlots: [{ key: "ceph", label: "سيفالو", category: "Radiograph", subtype: "Lateral Ceph", phase: "Initial", capture: "environment" }] },
  s(9, "cephmeas", "قياسات السيفالو", "تحليل محفوظ", "أكمل التحليل.", "افتح السيفالو.", Ruler, "data", "critical", "ceph", ["CephalometricMeasurements"]),
  s(10, "cast", "تحليل النماذج", "ALD/Pont", "أكمل النماذج.", "افتح تحليل النماذج.", Microscope, "data", "critical", "cast", ["CastAnalysis"]),
  s(11, "bolton", "تحليل بولتون", "نسب بولتون", "أكمل عند الحاجة.", "افتح النماذج.", Sigma, "data", "supporting", "cast", ["Bolton"]),
  { ...s(12, "problems", "قائمة المشاكل", "مشاكل هيكلية/سنية/وظيفية", "اكتب المشاكل.", "انسخها للمدخل الرسمي.", ListChecks, "data", "critical", "problems", ["ProblemList"]), draftKind: "problems" },
  { ...s(13, "diagnosis", "التشخيص", "هيكلي، سني، رخوة، وظيفي", "راجع التشخيص.", "انسخه لشاشة التشخيص.", ClipboardList, "data", "critical", "diagnosis", ["Diagnosis"]), approvedFlag: "diagnosis", draftKind: "diagnosis" },
  { ...s(14, "objectives", "أهداف العلاج", "أهداف علاجية", "اكتب الأهداف.", "انسخها للخطة.", Target, "data", "critical", "plan", ["TreatmentObjectives"]), draftKind: "objectives" },
  { ...s(15, "strategies", "استراتيجيات العلاج", "قلع/ارتكاز/توسيع", "اكتب الاستراتيجية.", "انسخها للخطة.", GitBranch, "data", "critical", "plan", ["TreatmentPlan"]), draftKind: "strategies" },
  { ...s(16, "plan", "خطة العلاج", "الجهاز، القلع، المدة", "اكتب الخطة.", "اعتمدها رسميًا.", FileText, "data", "critical", "plan", ["TreatmentPlan"]), approvedFlag: "plan", draftKind: "plan" },
  { ...s(17, "mechano", "الميكانيكا", "الحاصرات، السلك، الارتكاز", "اكتب المراحل.", "انسخها للخطة.", Wrench, "data", "critical", "plan", ["Mechanotherapy"]), draftKind: "mechano" },
  s(18, "visits", "الزيارات والتقدّم", "زيارة واحدة", "أضف الزيارات.", "افتح الزيارات.", CalendarClock, "data", "supporting", "visits", ["VisitProgress"]),
  { ...s(19, "final", "النتائج النهائية", "صور نهائية", "تستخدم عند اكتمال الحالة.", "التقط صور النهاية.", Trophy, "photos", "optional", undefined, ["FinalRecords"]), photoSlots: [{ key: "ff", label: "أمامي نهائي", category: "Extraoral", subtype: "Frontal", phase: "Final", capture: "user" }] },
  s(20, "retention", "الاحتفاظ", "المثبتات والتعليمات", "أضف الاحتفاظ.", "افتح الاحتفاظ.", Shield, "data", "optional", "retention", ["Retention"]),
  s(21, "generate", "إنشاء العرض", "مراجعة الجاهزية", "راجع ثم أنشئ.", "أنشئ العرض.", Presentation, "generate", "critical"),
];
const STATUS_META: Record<StepStatus, { label: string; cls: string; icon: typeof CheckCircle2 }> = { missing: { label: "ناقص", cls: "bg-gray-100 text-gray-500", icon: CircleDashed }, partial: { label: "جزئي", cls: "bg-amber-50 text-amber-700", icon: AlertCircle }, complete: { label: "مكتمل", cls: "bg-green-50 text-green-700", icon: CheckCircle2 }, approved: { label: "معتمد", cls: "bg-clinic-blue-50 text-clinic-blue", icon: BadgeCheck } };
const PRIORITY_META: Record<StepPriority, { label: string; cls: string }> = { critical: { label: "أساسي", cls: "bg-red-50 text-red-700" }, supporting: { label: "داعم", cls: "bg-blue-50 text-blue-700" }, optional: { label: "اختياري", cls: "bg-gray-100 text-gray-600" } };
const eq = (a?: string | null, b?: string) => (a ?? "").trim().toLowerCase() === (b ?? "").toLowerCase();
const isReady = (status: StepStatus) => status === "complete" || status === "approved";
const endpointSection = (kind?: DraftKind) => kind === "mechano" ? "mechanotherapy" : kind ?? "diagnosis";

export function OrthoCaseWizard({ caseId, patientId, onNavigate }: { caseId: string; patientId?: string; onNavigate: (tab: Tab) => void }) {
  const [open, setOpen] = useState<string | null>("patient");
  const [busy, setBusy] = useState(false);
  const [prepPhoto, setPrepPhoto] = useState<OrthoPhoto | null>(null);
  const overviewQ = useOrthoOverview(caseId);
  const photosQ = useOrthoPhotos(caseId);
  const defQ = useQuery({ queryKey: ["ortho-presentation-definition", caseId], enabled: !!caseId, retry: false, queryFn: async () => (await orthoService.getCasePresentationDefinition(caseId)).data });
  const o = (overviewQ.data ?? {}) as Record<string, unknown>;
  const photos = photosQ.data ?? [];
  const hasDataByType = useMemo(() => { const map: Record<string, boolean> = {}; (defQ.data as OrthoPresentationDefinition | undefined)?.slides.forEach((slide) => { map[slide.type] = slide.hasData; }); return map; }, [defQ.data]);
  const photosFor = (slot: PhotoSlot) => photos.find((photo) => (eq(photo.category, slot.category) || eq(photo.photoType, slot.category)) && eq(photo.subtype, slot.subtype) && (slot.phase === "Initial" ? !photo.treatmentPhase || eq(photo.treatmentPhase, "Initial") : eq(photo.treatmentPhase, slot.phase)));
  const statusFor = (step: WizardStep): StepStatus => { if (step.kind === "generate") return "complete"; if (step.approvedFlag === "diagnosis" && o.isDiagnosisApproved === true) return "approved"; if (step.approvedFlag === "plan" && o.isTreatmentPlanApproved === true) return "approved"; if (step.photoSlots) { const filled = step.photoSlots.filter((slot) => photosFor(slot)).length; if (filled === 0) return "missing"; return filled === step.photoSlots.length ? "complete" : "partial"; } return (step.slideTypes ?? []).some((type) => hasDataByType[type]) ? "complete" : "missing"; };
  const steps = STEPS.map((step) => ({ step, status: statusFor(step) }));
  const nonGenerateSteps = steps.filter((item) => item.step.kind !== "generate");
  const readyCount = nonGenerateSteps.filter((item) => isReady(item.status)).length;
  const criticalMissing = nonGenerateSteps.filter((item) => item.step.priority === "critical" && !isReady(item.status));
  const canGenerateFinal = criticalMissing.length === 0;
  const refresh = () => { photosQ.refetch(); defQ.refetch(); overviewQ.refetch(); };
  const generate = async (includeEmpty: boolean) => { if (!includeEmpty && !canGenerateFinal) { toast.error("أكمل الخطوات الأساسية أولًا"); setOpen(criticalMissing[0]?.step.key ?? "generate"); return; } setBusy(true); try { const res = await api.post(`/api/ortho-cases/${caseId}/case-presentation/pptx`, { includeEmptyOptionalSlides: includeEmpty }, { responseType: "blob" }); const url = window.URL.createObjectURL(new Blob([res.data], { type: "application/vnd.openxmlformats-officedocument.presentationml.presentation" })); const a = document.createElement("a"); a.href = url; a.download = `ortho-case-${caseId}.pptx`; document.body.appendChild(a); a.click(); a.remove(); window.URL.revokeObjectURL(url); toast.success("تم إنشاء العرض"); } catch { toast.error("تعذّر إنشاء العرض"); } finally { setBusy(false); } };
  return <div className="space-y-4" dir="rtl"><div className="rounded-xl border border-gray-200 bg-white p-4 shadow-sm"><div className="flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between"><div><h3 className="flex items-center gap-2 text-base font-bold text-clinic-navy"><Presentation className="h-5 w-5 text-clinic-blue" />معالج عرض الحالة</h3><p className="mt-1 text-xs text-gray-500">إدخال منظم للصور والتحليل والتشخيص والخطة.</p></div><div className="flex flex-wrap gap-2"><span className={cn("rounded-full px-3 py-1 text-xs font-bold", canGenerateFinal ? "bg-green-50 text-green-700" : "bg-amber-50 text-amber-700")}>{readyCount}/{nonGenerateSteps.length} جاهزة</span>{criticalMissing.length > 0 && <button type="button" onClick={() => setOpen(criticalMissing[0].step.key)} className="inline-flex items-center gap-1 rounded-full bg-red-50 px-3 py-1 text-xs font-bold text-red-700"><ArrowUpCircle className="h-3.5 w-3.5" />{criticalMissing[0].step.title}</button>}</div></div></div><ol className="space-y-2">{steps.map(({ step, status }) => { const meta = STATUS_META[status]; const priority = PRIORITY_META[step.priority]; const Icon = step.icon; const StatusIcon = meta.icon; const isOpen = open === step.key; return <li key={step.key} className="overflow-hidden rounded-xl border bg-white shadow-sm"><button type="button" onClick={() => setOpen(isOpen ? null : step.key)} className="flex w-full items-center gap-3 p-3 text-right hover:bg-gray-50"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-clinic-blue-50 text-clinic-blue"><Icon className="h-5 w-5" /></span><span className="min-w-0 flex-1"><span className="flex flex-wrap items-center gap-2"><span className="text-[11px] text-gray-400">{step.order}.</span><span className="text-sm font-semibold text-gray-800">{step.title}</span><span className={cn("rounded-full px-2 py-0.5 text-[10px]", priority.cls)}>{priority.label}</span></span><span className="block text-[11px] text-gray-500">{step.required}</span></span><span className={cn("inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[11px]", meta.cls)}><StatusIcon className="h-3.5 w-3.5" />{meta.label}</span><ChevronLeft className={cn("h-4 w-4 text-gray-300 transition", isOpen && "-rotate-90")} /></button>{isOpen && <div className="border-t border-gray-100 bg-gray-50/50 p-3"><StepGuidance step={step} status={status} />{step.kind === "patient" && <div className="mt-3"><Link href={patientId ? `/patients/${patientId}` : "#"} className="inline-flex items-center gap-1.5 rounded-lg bg-clinic-blue px-3 py-1.5 text-xs font-medium text-white"><Pencil className="h-3.5 w-3.5" />فتح ملف المريض</Link></div>}{step.kind === "data" && step.tab && <div className="mt-3 space-y-3">{step.draftKind && <ClinicalDraftPanel caseId={caseId} step={step} />}<button type="button" onClick={() => onNavigate(step.tab!)} className="inline-flex items-center gap-1.5 rounded-lg bg-clinic-blue px-3 py-1.5 text-xs font-medium text-white"><Pencil className="h-3.5 w-3.5" />فتح شاشة الإدخال</button></div>}{step.kind === "photos" && step.photoSlots && <div className="mt-3 grid gap-2 sm:grid-cols-2 xl:grid-cols-3">{step.photoSlots.map((slot) => <PhotoSlotCard key={slot.key} caseId={caseId} slot={slot} photo={photosFor(slot) ?? null} disabled={busy} onChanged={refresh} onPrepare={(photo) => setPrepPhoto(photo)} />)}</div>}{step.kind === "generate" && <div className="mt-3 flex gap-2"><button type="button" onClick={() => generate(false)} disabled={busy || !canGenerateFinal} className="rounded-lg bg-clinic-blue px-3 py-1.5 text-xs font-bold text-white disabled:opacity-50">إنشاء عرض نهائي</button><button type="button" onClick={() => generate(true)} disabled={busy} className="rounded-lg border bg-white px-3 py-1.5 text-xs">مسودة</button></div>}</div>}</li>; })}</ol><OrthoImagePreparationDialog caseId={caseId} photo={prepPhoto} open={prepPhoto !== null} onClose={() => setPrepPhoto(null)} onSaved={() => { setPrepPhoto(null); refresh(); }} /></div>;
}
function SummaryCard({ label, value, tone }: { label: string; value: string | number; tone: "success" | "warning" | "danger" }) { const cls = tone === "success" ? "border-green-100 bg-green-50 text-green-800" : tone === "danger" ? "border-red-100 bg-red-50 text-red-800" : "border-amber-100 bg-amber-50 text-amber-800"; return <div className={cn("rounded-lg border p-2", cls)}><div className="text-[11px] opacity-80">{label}</div><div className="text-sm font-bold">{value}</div></div>; }
function StepGuidance({ step, status }: { step: WizardStep; status: StepStatus }) { const statusLabel = STATUS_META[status].label; return <div className="grid gap-2 lg:grid-cols-3"><div className="rounded-lg border border-gray-200 bg-white p-2"><div className="text-[11px] font-semibold text-gray-500">المطلوب الآن</div><div className="mt-1 text-xs leading-5 text-gray-800">{step.minimum}</div></div><div className="rounded-lg border border-gray-200 bg-white p-2"><div className="text-[11px] font-semibold text-gray-500">إجراء الطبيب</div><div className="mt-1 text-xs leading-5 text-gray-800">{step.doctorAction}</div></div><div className="rounded-lg border border-gray-200 bg-white p-2"><div className="text-[11px] font-semibold text-gray-500">الحالة الحالية</div><div className="mt-1 text-xs leading-5 text-gray-800">{statusLabel}{step.priority === "critical" && !isReady(status) ? " — تمنع العرض النهائي" : ""}</div></div></div>; }
function ClinicalDraftPanel({ caseId, step }: { caseId: string; step: WizardStep }) { const storageKey = `ortho-wizard-draft:${caseId}:${step.key}`; const template = draftTemplate(step.draftKind); const [draft, setDraft] = useState(template); const [loading, setLoading] = useState(false); useEffect(() => { const saved = window.localStorage.getItem(storageKey); if (saved) setDraft(saved); }, [storageKey]); useEffect(() => { window.localStorage.setItem(storageKey, draft); }, [draft, storageKey]); const copy = async () => { await navigator.clipboard.writeText(draft); toast.success("تم نسخ المسودة"); }; const requestDraft = async () => { setLoading(true); try { const { data } = await api.post<ServerDraftResponse>(`/api/ortho-cases/${caseId}/a` + `i/clinical-draft`, { section: endpointSection(step.draftKind) }); const details = [data.draft, data.evidenceUsed?.length ? `Evidence: ${data.evidenceUsed.join(", ")}` : "", data.missingData?.length ? `Missing: ${data.missingData.join(", ")}` : "", data.warnings?.length ? `Warnings: ${data.warnings.join(", ")}` : "", data.disclaimer].filter(Boolean).join("\n\n"); setDraft((current) => current.trim() && current.trim() !== template.trim() ? `${current.trim()}\n\n---\n${details}` : details); toast.success("تم توليد مسودة الحالة"); } catch (error) { const message = (error as { response?: { data?: { message?: string } } }).response?.data?.message ?? "تعذر توليد المسودة"; toast.error(message); } finally { setLoading(false); } }; return <div className="rounded-xl border border-clinic-blue/20 bg-white p-3 shadow-sm"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-1.5 text-xs font-bold text-clinic-navy"><Clipboard className="h-3.5 w-3.5 text-clinic-blue" />مسودة سريرية</div><p className="mt-1 text-[11px] text-gray-500">الناتج مسودة تحتاج مراجعة واعتماد الطبيب.</p></div><div className="flex gap-1.5"><button type="button" onClick={requestDraft} disabled={loading} className="inline-flex items-center gap-1 rounded-lg bg-clinic-blue px-2.5 py-1.5 text-[11px] font-medium text-white disabled:opacity-50">{loading ? <Loader2 className="h-3.5 w-3.5 animate-spin" /> : <Sparkles className="h-3.5 w-3.5" />}اقتراح</button><button type="button" onClick={copy} className="rounded-lg border px-2.5 py-1.5 text-[11px]"><Clipboard className="inline h-3.5 w-3.5" /> نسخ</button></div></div><textarea value={draft} onChange={(e) => setDraft(e.target.value)} rows={8} className="mt-3 w-full rounded-lg border border-gray-200 bg-gray-50 p-3 text-xs leading-6 text-gray-800 outline-none focus:border-clinic-blue focus:bg-white" /><div className="mt-2 rounded-lg bg-amber-50 p-2 text-[11px] text-amber-800">لا تُحفظ رسميًا إلا بعد نسخها واعتمادها من الشاشة المرتبطة.</div></div>; }
function draftTemplate(kind?: DraftKind) { switch (kind) { case "problems": return "Skeletal problems:\n- \n\nDental problems:\n- \n\nSoft tissue problems:\n- \n\nFunctional problems:\n- "; case "diagnosis": return "Skeletal diagnosis:\n- \n\nDental diagnosis:\n- \n\nSoft tissue diagnosis:\n- \n\nFinal summary:\n- "; case "objectives": return "Treatment objectives:\n1. \n2. \n3. "; case "strategies": return "Treatment strategies:\n\nProblem:\nStrategy:\nRationale:\nAlternative:"; case "plan": return "Treatment plan:\n1. Appliance:\n2. Extraction decision:\n3. Anchorage:\n4. Duration:\n5. Retention:"; case "mechano": return "Mechanotherapy:\n\nAlignment:\n- \n\nCorrection:\n- \n\nFinishing:\n- "; default: return "Clinical draft:\n- "; } }
function PhotoSlotCard({ caseId, slot, photo, disabled, onChanged, onPrepare }: { caseId: string; slot: PhotoSlot; photo: OrthoPhoto | null; disabled: boolean; onChanged: () => void; onPrepare: (p: OrthoPhoto) => void }) { const inputRef = useRef<HTMLInputElement>(null); const [uploading, setUploading] = useState(false); const preparedUrl = typeof (photo as Record<string, unknown> | null)?.preparedImageUrl === "string" ? (photo as unknown as { preparedImageUrl?: string }).preparedImageUrl : undefined; const onFile = async (file: File | undefined) => { if (!file) return; setUploading(true); try { const form = new FormData(); form.append("file", file); const { data } = await api.post<{ url: string }>("/api/uploads", form, { headers: { "Content-Type": "multipart/form-data" } }); await orthoService.addPhoto(caseId, { photoUrl: data.url, category: slot.category, subtype: slot.subtype, photoType: slot.category, treatmentPhase: slot.phase, isSelectedForReport: true, caption: slot.label } as Partial<OrthoPhoto>); toast.success("تم حفظ الصورة"); onChanged(); } catch { toast.error("تعذّر رفع الصورة"); } finally { setUploading(false); if (inputRef.current) inputRef.current.value = ""; } }; return <div className="rounded-lg border border-gray-200 bg-white p-2.5"><div className="flex items-center justify-between gap-2"><div><span className="block text-xs font-semibold text-gray-700">{slot.label}</span><span className="text-[10px] text-gray-400">{slot.phase === "Final" ? "نهائية" : "بداية"}</span></div>{photo ? <span className="inline-flex items-center gap-1 rounded-full bg-green-50 px-1.5 py-0.5 text-[10px] text-green-700"><CheckCircle2 className="h-3 w-3" />محفوظة</span> : <span className="rounded-full bg-gray-100 px-1.5 py-0.5 text-[10px] text-gray-500">مطلوبة</span>}</div>{photo ? <img src={resolveImageUrl(preparedUrl ?? photo.photoUrl)} alt={slot.label} className="mt-2 h-28 w-full rounded-md object-cover" /> : <div className="mt-2 grid h-28 place-items-center rounded-md border border-dashed border-gray-200 bg-gray-50 text-[11px] text-gray-400">لم تُلتقط بعد</div>}<input ref={inputRef} type="file" accept="image/*" capture={slot.capture} className="hidden" onChange={(e) => onFile(e.target.files?.[0])} /><div className="mt-2 flex flex-wrap gap-1.5"><button type="button" disabled={disabled || uploading} onClick={() => inputRef.current?.click()} className="rounded-md bg-clinic-blue px-2.5 py-1 text-[11px] font-medium text-white disabled:opacity-50">{uploading ? "..." : photo ? "إعادة" : "التقاط / رفع"}</button>{photo && <button type="button" onClick={() => onPrepare(photo)} className="inline-flex items-center gap-1 rounded-md border border-gray-200 px-2.5 py-1 text-[11px] font-medium text-gray-600"><Upload className="h-3 w-3" />قص وتجهيز</button>}{preparedUrl && <span className="rounded-md bg-blue-50 px-2.5 py-1 text-[11px] text-blue-700">جاهزة</span>}</div></div>; }
