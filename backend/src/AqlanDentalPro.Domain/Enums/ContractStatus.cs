namespace AqlanDentalPro.Domain.Enums;

/// <summary>M2 FIX: Contract status enum — replaces magic string "active"/"completed"/"cancelled".</summary>
public enum ContractStatus
{
    Active = 0,
    Completed = 1,
    Cancelled = 2
}
