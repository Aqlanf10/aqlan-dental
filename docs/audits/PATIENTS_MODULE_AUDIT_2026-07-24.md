# تدقيق وحدة المرضى — 2026-07-24 (CORE-PAT)

تدقيق بقراءة الكود لوحدة المرضى: الخلفية، الواجهة، والترابط مع بقية الوحدات
والازدواجية. طُلب بتوجيه مالك مباشر ضمن مرحلة `CORE-00`.
خط الأساس وقت التدقيق: `main` عند `9909b16` (بعد PR #726)، 434 اختبار أمامي أخضر.

الترقيم `CORE-PAT-xxx` يُستخدم في التعليقات داخل الكود وفي رسائل الـcommits.

---

## 1) ما أُصلح في هذه الجولة (PR الأول)

| # | الخطورة | الموقع | العطل | الإصلاح |
|---|---------|--------|-------|---------|
| `CORE-PAT-001` | **P1 مالي** | `patients/[id]/page.tsx` | الملخّص كان يُجلب بـ`.catch(() => {})` والرصيد يُقرأ `?? 0` — فعند أي فشل تعرض بطاقة «الرصيد المالي» **صفرًا بثقة** ويختفي وسم «متبقي»، فيبدو المريض المدين خالص الحساب | `lib/patientSummary.ts` يفصل «مجهول» عن «صفر»، وتظهر رسالة عربية + إعادة محاولة بدل الرقم الكاذب |
| `CORE-PAT-004` | **P1 عطل** | `AppointmentsTab.tsx` | الـDTO يعلن `date`/`treatmentType` بينما الخادم يرسل `appointmentDate`/`appointmentType` → `formatArabicDate(undefined)` يرمي `RangeError` **فتسقط صفحة المريض كاملة** عند فتح تبويب المواعيد | تصحيح أسماء الحقول + تحصين `formatArabicDate` ضد أي مدخل غير صالح |
| `CORE-PAT-005` | **P1 عطل** | `PrescriptionsTab.tsx` | الاستجابة مغلّفة `{data,total}` والمكوّن يمرّرها كمصفوفة → `.map` يرمي `TypeError`؛ وكل الحقول (`date`/`status`/`items`) لا يرسلها الخادم أصلًا | فك التغليف + DTO مطابق للواقع (`createdAt`/`diagnosis`/`drugCount`) مع رابط للتفاصيل |
| `CORE-PAT-006` | **P1 أمني** | `AppointmentsController` | لا يطبّق `PatientAccessFilter`، فطبيب مرفوض على `/api/patients/{id}` كان يقرأ اسم المريض ورقم ملفه وهاتف المرافق وملاحظات مواعيده من `/api/appointments/patient/{id}` | إضافة الفلتر (نفس ما تطبّقه بقية متحكمات بيانات المرضى) |
| `CORE-PAT-003` | **P2 سريري** | `PatientService` + `PatientPortalService` | العمر كان `today.Year - dob.Year` بلا اعتبار لحدوث الميلاد → **زائد سنة لنحو نصف المرضى**، ويخالف ما تعرضه بوابة المريض. العمر يدخل في جرعات الأطفال وتوقيت التقويم | دالة واحدة نقية `Domain/Common/PatientAge.cs`، والبوابة تحسب على يوم العيادة |
| `CORE-PAT-007` | **P2 سريري** | `MedicalHistoryTab` | النموذج يكتب `Yes/No/N/A` (المعتمدة في مدقق الخادم) والتبويب يقرأ/يكتب `yes/no/na` → **مريضة حامل تظهر «لا ينطبق»**، والحفظ من التبويب يُرفض | قيَم موحّدة + قراءة غير حساسة لحالة الأحرف |
| `CORE-PAT-008` | **P2** | `OverviewTab` | خطأ أسبقية عوامل: `LABEL ?? status === "signed" ? …` → **كل صفوف الجدول الزمني تُعرض «موقع»** حتى الملغاة | إعادة التقويس (النسخة الصحيحة موجودة في `TimelineTab`) |
| `CORE-PAT-002` | **P2** | `TreatmentPlanTab` | فشل كتالوج الخدمات صامت → قائمة فارغة بلا تفسير تمنع إضافة خطوة | رسالة عربية مع إبقاء الإدخال اليدوي متاحًا |

---

## 1-ب) ما أُصلح في الشريحة الثانية (PR التالي — CORE-PAT-009..013)

| # | الخطورة | العطل | الإصلاح |
|---|---------|-------|---------|
| `CORE-PAT-009` | **P1 بيانات** | البوابة تكتب `NormalizedPhone` بصيغة `+967…` بينما الطاقم يكتب `967…` → فشل كشف التكرار وإنشاء ملف ثانٍ لنفس الشخص | البوابة تستخدم `PhoneNormalizer` القانوني + حارس تكرار برسالة عربية؛ صيغة `+` بقيت للإرسال عبر البوابات فقط |
| `CORE-PAT-010` | **P1 بيانات** | تحويل الحجز ينشئ `Patient` يدويًا بلا رقم ملف ولا تطبيع → التحويل الثاني يفشل 500 وكل عودة تنشئ ملفًا مكررًا | التحويل يمر عبر `PatientService.CreateAsync` (رقم + تطبيع + فرع + حساب بوابة) |
| `CORE-PAT-011` | **P1 أمني** | `SearchController` بحث ثانٍ بلا تصفية وصول ويعيد الهاتف للأطباء | تصفية المجموعات الثلاث بـ`GetAccessiblePatientIdsAsync` (fail-closed) وإخفاء الهاتف عن الأطباء |
| `CORE-PAT-012` | **P1 مالي** | رصيد البوابة يتجاهل الفواتير ويحسب العقود النشطة فقط بـ`Amount` → «المتبقي: 0» لمريض مدين | الكتلتان المكررتان حُذفتا وتفوّضان لـ`FinanceReadService` القانوني؛ وأُصلح في القانوني نفسه احتساب تكلفة العقود **الملغاة** (اتساقًا مع استثناء الفواتير الملغاة، في الملخّص وكشف الحساب معًا) |
| `CORE-PAT-013` | **P1 بيانات** | البريد المُدخل عند التسجيل يُفقد صامتًا (`EnsureLinkedUserAsync` قبل `SaveChanges` فلا ترى الحساب) | الحفظ أولًا ثم الربط؛ و`SetPatientEmailAsync` تُصلح الحسابات القديمة غير المربوطة بدل الإسقاط الصامت |

## 1-ج) ما أُصلح في الشريحة الثالثة (CORE-PAT-014..018)

| # | العطل | الإصلاح |
|---|-------|---------|
| `CORE-PAT-014` | حلقة إعادة توليد رقم المريض تفصل الجذر فقط → سجل التاريخ الطبي يبقى معلّقًا → 500 وضياع التسجيل عند التزامن | فصل الرسم البياني كاملًا (`DetachChild` جديدة في المستودع) |
| `CORE-PAT-015` | ملف المريض المؤرشف يُفتح لكن `/summary` يعيد 404 | فحص الوجود بـ`IgnoreQueryFilters` مطابقةً لسلوك الملف |
| `CORE-PAT-016` | تنبيه الطابور بالواتساب يُشترط على `Phone` بينما الإرسال الفعلي يستخدم `WhatsApp ?? Phone` | البوابة توحّدت مع رقم الإرسال الفعلي ويُبلَّغ الرقم الصحيح |
| `CORE-PAT-017` | «اليوم» في تحويل الحجز بتوقيت UTC → التحويل بين 12–3 فجرًا لا يدخل الطابور | `ClinicTimeProvider.ClinicToday()` |
| `CORE-PAT-018` | لا حقل `message` عربي في أخطاء الوسيط ولا في أخطاء التحقق (عنوان إنجليزي عام)، وStaging يسرّب نص الاستثناء الخام | `message` في كل استجابات الوسيط + مصنع استجابة تحقق يجمع رسائل المدققات العربية + حصر النص الخام بـDevelopment |

## 1-د) ما أُصلح في الشريحة الرابعة (CORE-PAT-019)

| # | الخطورة | العطل | الإصلاح |
|---|---------|-------|---------|
| `CORE-PAT-019` | **P1 أمني/مالي** | ثلاثة نماذج صلاحيات لرصيد المريض: ملخص الملف بأدوار مثبتة، ملخص الدفعات بصلاحية من القاعدة، وكشف الحساب بلا فحص دقيق؛ لذلك سحب الصلاحية من الإعدادات لا يغلق كل المسارات | توحيد `PatientsController` مع `PermissionGuard`: الملخص يتطلب `finance.patient_balance.view` وكشف الحساب يتطلب `finance.account_statement.view`، مع 403 عربي واختبارات منع رجوع |

## 1-هـ) ما أُصلح في الشريحة الخامسة (CORE-PAT-020)

| # | الخطورة | العطل | الإصلاح |
|---|---------|-------|---------|
| `CORE-PAT-020` | **P1 أمني** | التقرير رصد `/api/public/queue` المجهول، ثم كشف التنفيذ أن شاشة التلفاز الفعلية تستخدم أيضًا `/api/clinic-queue/display` المجهول وتعرض الاسم المختصر ورقم الملف كاملًا؛ حماية القديم وحده كانت ستترك التسريب الحقيقي قائمًا | إزالة `/clinic-display` من المسارات العامة؛ نقل واجهتها الكبيرة كما هي خلف طبقة مصادقة صغيرة تمرّر طلب الطابور عبر عميل JWT وتجديد الجلسة؛ إضافة حارس خلفي مستقل يفرض سياسة `StaffOnly` على المسارين حتى مع وجود `AllowAnonymous`؛ واعتماد يوم العيادة في المسار القديم، مع اختبارات 401/403 والسماح للطاقم |

## 2) المتبقي الموثّق — بالأولوية (لم يُنفَّذ بعد)

### بيانات (P1)

### اتساق (P2)
- «اليوم» ما يزال UTC في `AppointmentsController` و`PatientPortalService`
  — تحتاج المواضع مراجعة كل موضع على حدة لأن بعضها يغذي مقارنات مواعيد مستقبلية.
- **«حالة تقويم نشطة» بثلاثة تعريفات** (علم الحذف الناعم فقط / `Status==Active` /
  عدّاد ثالث) → حالة منتهية منذ سنتين تظهر نشطة في ملخّص اليوم.
- **`OverviewTab` وصفحة طباعة الملخّص** تقرآن 13 حقلًا لا يرسلها
  `GET /api/patients/{id}/summary` → «آخر زيارة» و«الموعد القادم» و«تنبيهات طبية»
  فارغة دائمًا في أول شاشة يراها الطبيب وفي الملف المطبوع.
- **الدفعات مقطوعة عند 20** (الصفحة الافتراضية) في تبويب الدفعات وفي **كشف الحساب
  المطبوع**، مع إجمالي يُحسب في الواجهة من صفحة واحدة فقط.
- `PatientTable`: بحث فوري بلا `AbortController` ولا رقم تسلسلي (نتيجة قديمة
  تستبدل أحدث)، وترقيم صفحات يعرض 3 أزرار بلا تالي/سابق → **الصفحة الرابعة فما
  بعدها غير قابلة للوصول**.
- إخفاقات صامتة إضافية: `PaymentsTab` (تعرض «إجمالي المدفوعات 0»)، `OverviewTab`
  الجدول الزمني، `OrthoSurgicalTab`، `MessagesTab`، `PortalAccessTab` (تفسّر أي
  خطأ كـ«لا يوجد حساب»)، تصدير `PatientTable`.

### ازدواجية (P3) — أبرزها
- **رصيد المريض: 6 تنفيذات** بقواعد مختلفة (`FinanceReadService` هي المرجع).
- **بحث المرضى: 3 تنفيذات**، **تطبيع الهاتف: 5**، **حساب العمر: 6** (وُحِّد اثنان منها هنا).
- **تنسيق اسم المريض: 4 في الخلفية + 5 في الواجهة** بقواعد مختلفة (مسافة مزدوجة).
- **`hooks/usePatients.ts` ميت بالكامل** (203 سطر، صفر استيراد) ويحمل مسارات
  تخالف ما تستدعيه الشاشات فعلًا.
- `PatientSummary` معرّف 4 مرات في الواجهة بثلاث أشكال، وأحدها يعلن الحقول
  المالية غير قابلة للـnull بينما الخادم يعيدها `null` للأطباء.
- endpoint ميت `/api/patients/{id}/portal-credentials`، ومسارا أرشفة مزدوجان.
- بادئة رقم المريض `"GM"` تُقرأ من `IConfiguration` لا من جدول `Settings`
  (مخالفة قاعدة «لا hardcoding» عمليًا، إذ لا مفتاح لها في أي ملف إعدادات).
- `Payment.PatientId` بلا FK مُعرّف صراحةً (خلافًا لبقية كيانات المالية)،
  و`BookingRequest.ConvertedToAppointmentId` ليس FK إطلاقًا.
- التحقق من وجود المريض عند إنشاء السجلات التابعة غير متسق: غائب في
  `LabOrders`/`ClinicalPhotos`/`Payments`/`Contracts`/`OrthoCases` → يمكن إنشاؤها
  على ملف مؤرشف.

### قرار تصميم مطلوب (ليس عطلًا)
حالة المريض «الآن» مخزّنة في ثلاثة أماكن مستقلة: `Appointment.Status`،
`ClinicQueueItem.Status`، و`Visit.CheckoutStatus`. المزامنة اختيارية بالتصميم
(موثّقة في `SyncAppointmentStatus`)، وإضافة مريض للطابور لا تحدّث الموعد إطلاقًا.
توحيدها قرار مالك، لا إصلاح.

---

## 3) ما فُحص ووُجد سليمًا

`PatientAccessFilter` نفسه (رسالة 403 عربية + تدقيق)؛ فحص الوصول موجود على كل
endpoints بيانات المرضى داخل `PatientsController`؛ `GetList` مُصفّاة فعلًا
بـ`GetAccessiblePatientIdsAsync` وتتجاهل `doctorId` القادم من العميل؛ الأطباء
يتلقون `PatientClinicalDto` المخفّض بلا هاتف ولا مالية؛ مرشّح الحذف الناعم مطبّق
عامًا؛ `GetLastVisitDatesAsync` دفعة حقيقية بلا N+1؛ `PatientCombobox` نموذج
مثالي (إلغاء + تسلسل + رسالة صادقة)؛ قاعدة التاريخ المحلي (`localDateString`)
مطبّقة بلا مخالفة واحدة في كل ملفات المرضى؛ فخّ `LabOrders.DoctorId` معالج
صحيحًا ولا نظير له في مسارات المرضى؛ تكرار شاشات ملف المريض سبق حلّه
(`/patient-journey` أصبح إعادة توجيه).
