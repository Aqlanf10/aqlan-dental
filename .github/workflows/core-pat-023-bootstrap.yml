name: CORE-PAT-023 Bootstrap

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.event.pull_request.head.ref == 'agent/core-pat-023-portal-access-errors' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/core-pat-023-portal-access-errors
          fetch-depth: 0

      - name: Apply portal access patch
        run: python scripts/core_pat_023_patch.py

      - name: Commit patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(patients): CORE-PAT-023 surface portal account load failures"
          git push origin HEAD:agent/core-pat-023-portal-access-errors
