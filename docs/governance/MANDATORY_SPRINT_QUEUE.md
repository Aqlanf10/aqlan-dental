# طابور السبرنتات الإلزامي — Mandatory Sprint Queue

**هذا الملف هو مصدر الحقيقة الوحيد لترتيب العمل على Aqlan Dental Pro.**
يُلغي هذا الملف — بأثر فوري — أي ترتيب سبرنتات سابق ورد في `CLAUDE.md` أو
`docs/agent-audit/development-roadmap.md` أو `docs/technical-debt-register.md`
(هذان الملفان مؤرشفان الآن كسجل تاريخي فقط — انظر ملاحظة الأرشفة في كل منهما).

## القاعدة الإلزامية (تنطبق على أي وكيل: Claude، Codex، Gemini، OpenCode، أو أي نموذج آخر)

1. **قبل أي عمل تطويري**، اقرأ هذا الملف كاملًا.
2. ابحث عن **أول عنصر** بحالة `🔴 محجوب` أو `🟡 جاهز` أو `🔵 قيد التنفيذ` بالترتيب من الأعلى للأسفل.
3. **هذا هو العنصر الوحيد المسموح العمل عليه.** لا يجوز القفز لعنصر لاحق حتى لو بدا أسهل أو أكثر إثارة، إلا ضمن الاستثناءات أدناه.
4. عند إنجاز عنصر (PR مدموج + الحالة `الخروج` محقّقة)، حدّث حالته إلى `✅ منجَز` في نفس الـPR أو بـPR توثيقي فوري تالٍ، ثم انتقل للعنصر التالي.
5. كل PR ينفّذ عنصرًا من هذا الطابور **يجب أن يذكر معرّف العنصر** (مثل `SEQ-04`) في عنوان الكوميت/الـPR.

### الاستثناءات الوحيدة المسموحة (لا تحتاج إذنًا لتجاوز الترتيب)

- **حوادث الإنتاج الحرجة (P0)**: عطل حي يمنع الاستخدام الفعلي في العيادة الآن (مثل توقف نشر Railway الحالي — انظر SEQ-00). هذه تُعالَج فورًا بصرف النظر عن الطابور، ثم تُسجَّل نتيجتها هنا.
- **فحص/تدقيق للقراءة فقط** (QA rounds، code review، بحث) — لا يغيّر كودًا، فمسموح دائمًا.
- **إكمال عمل موجود بالفعل قيد التنفيذ** (PR مفتوح لعنصر أعلى في الطابور) — أولوية أعلى من فتح عمل جديد.
- **إصلاح تعارض دمج (merge conflict) على PR موجود** — عمل ميكانيكي لا يفتح نطاقًا جديدًا.

أي عمل خارج هذه الاستثناءات وخارج العنصر الأول غير المنجز = **مخالفة لسياسة الوكلاء** ويجب على أي وكيل لاحق أن يوقفه ويُعيده للطابور الصحيح.

---

## الطابور (بالترتيب الإلزامي)

### 🔴 SEQ-00 — فك توقف نشر Railway (بوابة تحجب كل شيء آخر عمليًا)

- **الحالة:** 🔴 محجوب على إجراء المالك (مؤكَّد حيًا حتى جولة QA الخامسة، 2026-07-05/06)
- **لماذا يتصدر الطابور:** الإنتاج يشغّل بناءً أقدم من PR #598 بينما `main` يحتوي إصلاحات مدمجة منذ 4+ أيام (`dashboard/stats`، `patient-journey/{id}/daily-summary`، `contracts` كلها 500 حيًا رغم إصلاحها على `main`). أي سبرنت جديد يُبنى فوق حالة لا يمكن التحقق منها حيًا حتى يُحل هذا.
- **الإجراء المطلوب:** قائمة الفحص خطوة-بخطوة في `docs/qa/PRODUCTION_OWNER_QA_ROUND_1.md` (قسم «تشخيص توقف نشر Railway»)، مع أدوات تشخيص جاهزة مدموجة (`GET /api/finance-v3/diagnostic/startup-hotfixes` و`.../diagnostic/schema-columns`، AdminOnly).
- **معيار الخروج:** طلب واحد إلى `/api/dashboard/stats` من الإنتاج يعيد 200.
- **من ينفذ:** المالك (وصول Railway dashboard) — لا وكيل كود يملك صلاحية هذا.

---

### ✅ SEQ-01 — أمان جلسات بوابة المرضى

- **الحالة:** ✅ منجَز (SEC-10)
- **الدليل:** `PatientPortalController.cs` يصدر `refresh_token` كـ HttpOnly+Secure+SameSite cookie؛ `patientAuthStore.ts` لا يخزّن التوكن في localStorage.
- **لا عمل متبقٍ.**

### ✅ SEQ-02 — تعيينات غرف الأطباء

- **الحالة:** ✅ منجَز (هذه الجلسة، PR سابق مدموج)
- **الدليل:** `Doctor.DefaultClinicRoomId` + `DoctorRoomResolver` + استخدامه في `ClinicQueueController.CallPatient/RecallPatient` + واجهة `doctors/page.tsx`.
- **لا عمل متبقٍ.**

### 🟡 SEQ-03 — شاشة إدارة المستخدمين/الصلاحيات الموحدة (فجوة مؤكدة)

- **الحالة:** 🟡 جاهز — الفحص الحي (QA4-03 وجولة QA الخامسة) أكد أن `/settings/users`، `/settings/permissions`، `/users` كلها 404، ولا يوجد رابط في مركز الإعدادات إليها.
- **Spec المرجعي:** `specs/008-users-roles-permissions/requirements.md` (اقرأه وحدّثه أولًا إن كان السلوك المطلوب غير موثّق).
- **الملفات المرجّحة:** `UsersController.cs` (الباك إند موجود ويُستخدم فعليًا من صفحات أخرى)، `frontend/src/app/(dashboard)/settings/_components/UsersTab.tsx` (موجود بالفعل — تحقق أولًا: هل التبويب فعليًا معطّل/مخفي أم أن الـ404 من مسار مباشر غير مستخدم؟) قبل إنشاء أي مسار جديد.
- **معيار الخروج:** مسار واحد قابل للوصول من مركز الإعدادات يعرض المستخدمين والأدوار والصلاحيات دون تكرار شاشة.
- **ملاحظة:** تحقق أولًا مما إذا كان `UsersTab`/`RolesTab` الموجودان في `settings/page.tsx` يغطيان هذه الحاجة فعليًا — إن كانا كذلك، فالإصلاح هو ببساطة توضيح أن `/settings/users` رابط قديم يجب أن يُوجَّه (redirect) لتبويب `settings?tab=users`، لا شاشة جديدة (اتبع نمط `hr/page.tsx` من QA3-05).

### 🟡 SEQ-04 — إكمال TD-021 PR A4 (تفكيك FinanceService المتبقي)

- **الحالة:** 🟡 جاهز — PR #606 مفتوح منذ 2026-07-03، مبني على قاعدة قديمة (+257870/-977 عبر 1070 ملف — منحرف كليًا عن `main` الحالي). `FinanceService.cs` اليوم 1623 سطرًا؛ A1–A3 (`InvoiceLedgerService`، `FinanceReadService`، `ContractService`) مدموجة، لكن `PaymentService`/`SupplierRefundService`/`FinanceLedgerWriter` (A4) غير موجودة على `main`.
- **الإجراء:** أعد بناء الفرع من `main` الحالي (نفس أسلوب إصلاح تعارض PR #620 في هذه الجلسة: فرع جديد من `main` + `cherry-pick` للكوميتات الحقيقية فقط، وليس merge أعمى) بدل محاولة حل التعارض المباشر على فرع منحرف بهذا الحجم.
- **معيار الخروج:** `FinanceService.cs` ≈ 67 سطرًا (جسر `TryMarkInvoicePaidAsync` فقط)، `dotnet test` أخضر بالكامل، لا تغييرات سلوك مالي.
- **تحذير:** هذا يمسّ المالية — **نماذج قوية فقط** (انظر `docs/governance/MULTI_MODEL_AGENT_RULES.md`).

### 🟡 SEQ-05 — توحيد المنطقة الزمنية في وحدة الطابور والرحلة (QA5-03)

- **الحالة:** 🟡 جاهز — مكتشف ومُوثّق حيًا في جولة QA الخامسة (PR #620). عائلة كاملة من `DateOnly.FromDateTime(DateTime.UtcNow)` في:
  - `ClinicQueueController.cs` (الأسطر 53، 155، 815، 1093، 1272، 1364 وقت الاكتشاف)
  - `CheckoutService.cs` (186، 330، 593، 717)
  - `PatientJourneyService.GetDailySummary` (446)
- **النمط المرجعي:** PR #582 (`OverdueNotificationJob` → `ClinicTimeProvider.ClinicToday()`/`ToUtcRange`) و SEQ-05 نفسه جزئيًا في `ClinicQueueController.StartVisit` (أُصلح في PR #620).
- **معيار الخروج:** كل المواقع أعلاه تستخدم `ClinicTimeProvider.ClinicToday()`؛ اختبار جديد يثبت أن عنصر انتظار مُنشأ الساعة 01:00 بتوقيت اليمن (22:00 UTC اليوم السابق) يُحسب ضمن يوم العيادة الصحيح.
- **خطر الانحراف:** هذه دوال شديدة الاستخدام (كل عملية طابور/تحصيل يوميًا) — غطِّ بكامل الاختبارات الموجودة قبل الدمج ولا تُغيّر التوقيع العام للدوال.

### 🟡 SEQ-06 — تشخيص وإصلاح انحراف نوع عمود Enum في المصروفات (بعد SEQ-00)

- **الحالة:** 🟡 جاهز بشرط — يعتمد على نجاح SEQ-00 أولًا (أداة `GET /api/finance-v3/diagnostic/schema-columns` من QA4-08 ستكشف العمود المنحرف تلقائيًا فور أول نشر ناجح).
- **الأثر:** `GET /api/expenses` و`GET /api/finance-v3/expenses` يعيدان 500 حيًا (مؤكَّد جولة QA الخامسة).
- **لا تخمين:** لا تكتب أي إصلاح لهذا العنصر قبل قراءة نتيجة تشخيص `schema-columns` بعد نشر ناجح — قد يكون العمود `Category`/`ApprovalStatus` integer-vs-varchar (نفس فئة إصلاح `QA-602` للموردين).

### 🟢 SEQ-07 — تحسينات رسائل الخطأ الصامتة في التشغيل اليومي (QA3-02/QA5-04 family)

- **الحالة:** 🟢 قابل للتنفيذ فورًا (لا يعتمد على SEQ-00) — لكن أقل إلحاحًا من SEQ-03..06.
- **المشكلة:** إجراءات الرحلة (تسجيل وصول/نداء/إلخ) تعرض توست عام «فشل إتمام العملية» بدل رسالة الخادم العربية الفعلية عند الفشل.
- **الملفات:** مكوّنات إجراءات `daily-operations` (`JourneyContextMenu.tsx` وما شابه) — مرّر `err.response?.data?.message` للتوست بدل نص ثابت.
- **معيار الخروج:** فشل متعمد (مثلاً قفزة حالة غير صالحة) يُظهر رسالة الخادم العربية الفعلية في الواجهة.

### 🔵 SEQ-08 — تحسين وحدة التقويم (أولوية المالك المعلنة — الأهم بعد الاستقرار)

- **الحالة:** 🔵 قيد التنفيذ الجزئي — الذكاء الاصطناعي لاكتشاف نقاط السيفالو أصبح ثنائي المزود (Gemini + Anthropic، PR #599). تحليل الموديلات (7 طرق من تطبيق الأندرويد، PR #614) منجَز.
- **المرجع:** `docs/ortho-module/MASTER-PLAN.md` — **تحذير:** هذا الملف كان موصوفًا سابقًا بأنه غير مطابق للحالة الفعلية للكود (stale). **لا تعتمد عليه لتحديد "التالي" — دقّق الحالة الفعلية بقراءة الكود أولًا** (نفس المنهجية المتبعة في هذا الطابور نفسه).
- **الخطوة التالية المقترحة (تحتاج تأكيدًا بقراءة كود حية قبل البدء):** مراجعة `specs/004-orthodontics-workspace/requirements.md` لتحديد أي متطلب موثّق لم يُنفَّذ بعد، بدل افتراض ترتيب من مستند خارج SpecKit.

### 🟢 SEQ-09 — تنظيف فروع Git المتراكمة والـPRs المسودة القديمة

- **الحالة:** 🟢 قابل للتنفيذ فورًا (قراءة/تنظيف فقط، لا يمسّ الكود التشغيلي)
- **الاكتشاف:** PR #407 (مسودة، بلا نشاط منذ 2026-06-16) وPR #363 (مسودة، بلا نشاط منذ 2026-06-13) — يُرجَّح تجاوزهما بعمل لاحق.
- **الإجراء:** `./scripts/list-merged-branches.sh` (وضع القراءة فقط أولًا) + سؤال المالك صراحة قبل إغلاق أي PR مسودة أو حذف أي فرع غير مدموج (لا تُغلق/تحذف بدون تأكيد صريح — انظر قواعد الأمان في تعليمات النظام).

---

## آلية التحديث

- كل PR ينفّذ عنصرًا هنا **يُحدّث حالته في نفس الـPR** (`🔴/🟡/🔵` → `✅ منجَز` + رابط الـPR ومعيار الخروج المحقَّق).
- إذا اكتشف وكيل عنصر عمل جديدًا غير مدرَج هنا (من QA أو من طلب مالك)، يُضاف **في آخر الطابور** بحالة مبدئية `🟡 جاهز` (أو `🔴 محجوب` مع سبب الحجب)، ولا يُنفَّذ فورًا إلا إذا كان الاستثناء P0 ينطبق.
- `docs/governance/SPEC_TO_CODE_TRACEABILITY.md` يبقى سجل الأدلة التفصيلي (ماذا وُجد، أين الكود، ما الاختبار)؛ هذا الملف هو **الترتيب** فقط.
