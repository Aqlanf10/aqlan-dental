name: Encoding Guard

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: write

jobs:
  w05-typescript-fix:
    if: github.head_ref == 'codex/w05-clinic-business-date' && github.actor != 'github-actions[bot]'
    name: Apply W05 TypeScript Fix Once
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/w05-clinic-business-date
          fetch-depth: 0

      - name: Apply TypeScript narrowing fix
        run: python scripts/w05_fix_frontend_narrowing.py

      - name: Restore workflow and commit fix
        shell: bash
        run: |
          git checkout origin/main -- .github/workflows/encoding-guard.yml
          rm -f scripts/w05_fix_frontend_narrowing.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(journey): preserve appointment id narrowing for override retry"
          git push origin HEAD:codex/w05-clinic-business-date

  mojibake:
    name: Arabic Mojibake Guard
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check Arabic UTF-8 text is not corrupted
        run: bash scripts/check-mojibake.sh

      - name: Check cephalometric AI documentation contract
        shell: pwsh
        run: ./scripts/check-ceph-ai-docs.ps1
